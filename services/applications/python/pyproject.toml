[project]
name = "experimentops-python"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.8.0",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "apps/analysis-api",
    "apps/analysis-worker",
    "packages/experiment-runtime",
]
